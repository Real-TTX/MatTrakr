# .NET build output
bin/
obj/
out/
publish/

# Rider / VS / VS Code
.vs/
.idea/
*.user
*.suo
.vscode/

# Local runtime data (SQLite DB, mounted config) — root-anchored so it does NOT
# match the C# source folder src/MatTrakr/Data (Git is case-insensitive on Windows).
/data/
*.db
*.db-shm
*.db-wal

# Local secrets / env
*.env
.env
appsettings.Local.json

# OS
Thumbs.db
.DS_Store
